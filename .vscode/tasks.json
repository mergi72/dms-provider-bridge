{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Bridge Server",
      "type": "shell",
      "command": "$env:PYTHONPATH = Join-Path $PWD 'src'; $env:DMS_PROVIDER_MACHINE_CONFIG_DIR = Join-Path $PWD 'config'; $env:DMS_PROVIDER_USER_CONFIG_DIR = Join-Path $env:APPDATA 'DMS Provider\\config'; $listenPids = @(Get-NetTCPConnection -LocalPort 8765 -State Listen -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique); if ($listenPids.Count -gt 0) { Write-Host 'Bridge server already running on port 8765.' } else { Write-Host \"Machine config: $env:DMS_PROVIDER_MACHINE_CONFIG_DIR\"; Write-Host \"User config: $env:DMS_PROVIDER_USER_CONFIG_DIR\"; .\\.venv312\\Scripts\\python.exe -u -m dms_provider_bridge.main }",
      "isBackground": true,
      "problemMatcher": []
    },
    {
      "label": "Stop Bridge Server",
      "type": "shell",
      "command": "$listenPids = @(Get-NetTCPConnection -LocalPort 8765 -State Listen -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique); foreach ($procId in $listenPids) { Stop-Process -Id $procId -Force -ErrorAction SilentlyContinue }",
      "problemMatcher": []
    },
    {
      "label": "Restart Bridge Server",
      "type": "shell",
      "command": "$env:PYTHONPATH = Join-Path $PWD 'src'; $env:DMS_PROVIDER_MACHINE_CONFIG_DIR = Join-Path $PWD 'config'; $env:DMS_PROVIDER_USER_CONFIG_DIR = Join-Path $env:APPDATA 'DMS Provider\\config'; $listenPids = @(Get-NetTCPConnection -LocalPort 8765 -State Listen -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique); foreach ($procId in $listenPids) { Stop-Process -Id $procId -Force -ErrorAction SilentlyContinue }; Write-Host \"Machine config: $env:DMS_PROVIDER_MACHINE_CONFIG_DIR\"; Write-Host \"User config: $env:DMS_PROVIDER_USER_CONFIG_DIR\"; .\\.venv312\\Scripts\\python.exe -u -m dms_provider_bridge.main",
      "isBackground": true,
      "problemMatcher": []
    }
  ]
}

