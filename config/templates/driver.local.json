{
    "_comment": "Template for user driver local overrides. Copy as <driver>.local.json or drivers/<driver>.local.json and replace driver_name with the concrete driver key.",

    "key": "driver_name",

    "driver_name": {
        "base_url": "https://dms.example.com",

        "timeouts": {
            "requestSeconds": 60,
            "downloadSeconds": 300,
            "uploadSeconds": 7200
        },

        "debug": {
            "enable": false,
            "path": "%APPDATA%\\DMS Provider\\logs"
        }
    }
}
