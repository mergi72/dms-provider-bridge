"""Backward-compatible provider package.

Concrete implementations moved to ``dms_provider_bridge.drivers``. This package
keeps older provider-era imports working during the 0.8.x naming refactor.
"""

from __future__ import annotations
